-- move this to config.lua and choose the script according to the device you want to target

--[[
reactord.config = {
	access_token = "access token grabbed from the web interface, see kalkafox/reactord-web",
  type = "choose from any of the folders",
  id = "1", -- not needed if you are passing access_token
  url = "ws://foo.bar/baz"
}
--]]
